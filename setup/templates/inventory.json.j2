{
  "{{ ansible_system }}": {
    "hosts": {
      "localhost": {
        "ansible_connection": "local",
        "hostname": "{{ hostname }}",
        "email": "{{ email }}",
        "work_system": {{ work_system }}
      }
    }
  }
}
