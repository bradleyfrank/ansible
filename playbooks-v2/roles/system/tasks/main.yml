---

- name: Set hostname
  ansible.builtin.hostname:
    name: "{{ system_hostname }}"
  become: true
  when: system_hostname != ansible_facts['hostname']
  tags: [system, hostname]

- name: Include system-specific tasks
  ansible.builtin.include_tasks:
    file: "{{ ansible_facts['system'] }}.yml"
  tags: always
