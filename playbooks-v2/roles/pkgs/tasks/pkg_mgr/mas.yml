---

- name: Install mas formula
  community.general.homebrew:
    name: mas
  tags: [packages, mas]

- name: Install apps via mas
  community.general.mas:
    id: "{{ pkgs_mas_apps }}"
    upgrade_all: true
  tags: [packages, mas]
