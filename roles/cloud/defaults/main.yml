---

cloud_krew_bin: "{{ global_homebrew_prefix }}/bin/kubectl krew"

cloud_krew_plugins:
  - cert-manager
  - deprecations
  - get-all
  - neat
  - stern

cloud_packages:
  - kubectl
  - krew
  - k9s
  - kube-linter
