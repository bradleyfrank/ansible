---

zsh_chmod_directories:
  - "{{ global_homebrew_prefix | default('/opt/homebrew') }}/share"
  - "{{ global_homebrew_prefix }}/share/zsh"
  - "{{ global_homebrew_prefix }}/share/zsh/site-functions"
