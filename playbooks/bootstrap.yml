---

- name: Bootstrap localhost system
  hosts: localhost
  tasks:
    - name: Install system packages
      import_role:
        name: packages

    - name: Apply dotfiles
      import_role:
        name: dotfiles

    - name: Configure system settings
      import_role:
        name: system
