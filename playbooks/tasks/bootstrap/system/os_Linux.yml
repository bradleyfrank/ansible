---
- name: Set user shell to zsh and add to group 'cdrom'
  ansible.builtin.user:
    name: "{{ ansible_user_id }}"
    shell: /usr/bin/zsh
    groups: cdrom
    append: true
  become: true
  tags: user

